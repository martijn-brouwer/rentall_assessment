export const environment = {
  production: false,

  /*
  Note: Cannot retrieve json data files directly from rentall server, because CORS is not correctly set up. Copied json files to local directory.
  You can find a screenshot of the error message at /assets/images/cors_error.png
  */
  backend: '/assets/jsons/'



};
